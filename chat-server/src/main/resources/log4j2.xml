<Configuration>
    <Properties>
        <Property name="basePath">logs</Property>
    </Properties>
    <Appenders>
        <Console name="stdout" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss,SSS} %-5level ${PID:- } --- [%t] %-40.40logger{39} : %n%m%throwable"/>
        </Console>

        <File name="ChatServer-file" fileName="${basePath}/chatserver-logfile.log">
            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss,SSS} %-5level ${PID:- } --- [%t] %-40.40logger{39} : %m%n%throwable"/>
        </File>

        <RollingFile name="ChatServer-rollingFile" fileName="${basePath}/chatserver-rolling.log"
                     filePattern="${basePath}/chatserver-rolling-%d{MM-dd-yy}_%i.log.gz">
            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss,SSS} %-5level ${PID:- } --- [%t] %-40.40logger{39} : %m%n%throwable"/>
            <Policies>
                <SizeBasedTriggeringPolicy size="1M"/>
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>

        <Logger name="ru.gb.core" level="DEBUG" additivity="false">
            <AppenderRef ref="ChatServer-file"/>
            <AppenderRef ref="ChatServer-rollingFile"/>
            <AppenderRef ref="stdout"/>
        </Logger>

        <Root level="INFO" additivity="true">
            <AppenderRef ref="stdout"/>
        </Root>

    </Loggers>
</Configuration>